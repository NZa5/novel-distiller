# 安装

1. 把 `novel-distiller` 文件夹复制或克隆到宿主 Agent 支持的 Skill 目录。
2. 保持 `SKILL.md` 和 `references/` 的相对位置不变。
3. 如果宿主只在启动时发现 Skill，请重新加载或重启宿主。
4. 在处理重要文本前，先确认宿主能够列出或调用 `novel-distiller`。

不同宿主的 Skill 目录和调用语法并不相同，而且可能随版本变化。请以宿主的当前文档和实际发现结果为准，不要假设某一条路径或命令在所有环境都有效。

Novel Distiller 本身不需要 Python 包、单独的 API Key 或构建步骤；宿主 Agent 仍可能需要自己的账号、模型权限和附件读取能力。

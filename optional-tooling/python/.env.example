# OpenAI-compatible provider configuration (optional product only)
# This file is never auto-loaded. Pass --config explicitly.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_KEY is supplied out-of-band only when explicitly enabling remote mode.

# Optional Python tooling

Version 0.3.0 retains `output_format: legacy-0.2`. It uses finite sampling and does not implement the Skill checkpoint/resume protocol or canonical Schema 2.0.

Install with `pip install -e "optional-tooling/python[test]"`. Remote model use is optional and requires explicit consent.
